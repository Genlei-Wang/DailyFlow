# DailyFlow

<div align="center">

**轻量级 Windows 自动化助手**

零依赖 • 无需管理员权限 • 纯离线运行

[![Build Status](https://github.com/yourusername/dailyflow/workflows/Build%20DailyFlow/badge.svg)](https://github.com/yourusername/dailyflow/actions)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

</div>

---

## 项目简介

DailyFlow 是一款专为银行、保险等国央企内网环境设计的自动化助手，帮助基层职员自动化日常重复性操作。

### 核心特性

- **🚀 零依赖**：单文件绿色软件，无需安装任何运行时或框架
- **🔒 安全可靠**：无需管理员权限，纯离线运行，不联网
- **💡 简单易用**：录制操作、设定时间，自动执行
- **⚡ 轻量快速**：文件大小 < 5MB，启动速度 < 2 秒
- **🎯 精准回放**：支持变速回放（0.5x - 1.0x），适应不同系统响应速度
- **⏰ 定时任务**：每日自动执行，支持开机自启动

## 使用场景

适用于需要每天重复执行的固定操作流程，例如：

- 每日晨报生成
- 定时数据导出
- 批量表单填写
- 系统巡检操作
- 其他重复性工作

## 快速开始

### 下载

从 [Releases](https://github.com/yourusername/dailyflow/releases) 页面下载最新版本的 `DailyFlow.exe`。

### 使用步骤

#### 1. 录制操作流程

1. 运行 `DailyFlow.exe`
2. 点击主窗口的 **"🔴 录制 (F8)"** 按钮
3. 执行你需要自动化的操作流程
4. 再次点击录制按钮（或按 F8）停止录制
5. 录制数据自动保存到 `task.json`

#### 2. 测试回放

1. 点击 **"🟢 回放 (F12)"** 按钮
2. 程序将自动重复你录制的操作
3. 如果系统响应慢，可调整 **"速度"** 滑动条到 0.5x - 0.8x

#### 3. 设置定时任务

1. 在配置区域设置 **"执行时间"**（如 08:30）
2. 勾选 **"每日启用"** 复选框
3. 可选：勾选 **"开机自启"** 实现开机后自动待命
4. 最小化程序到托盘，程序将在设定时间自动执行任务

## 功能说明

### 主界面

```
┌─────────────────────────────────┐
│ ⚠️ 运行期间请保持屏幕常亮，勿锁屏 │
├─────────────────────────────────┤
│      下次运行: 明早 08:30         │
├─────────────────────────────────┤
│  🔴 录制 (F8)   🟢 回放 (F12)    │
├─────────────────────────────────┤
│ 配置                             │
│ 执行时间: [08:30] ☑ 每日启用    │
│ 速度: [━━━━━○━━━━] 1.0x         │
│ ☑ 开机自启                       │
└─────────────────────────────────┘
```

### 热键支持

- **F8**：开始/停止录制
- **F12**：开始/停止回放

热键在程序最小化到托盘时也能正常工作。

### 托盘菜单

右键点击托盘图标：

- **显示主界面**：打开主窗口
- **关于**：查看版本信息
- **退出程序**：完全退出 DailyFlow

### 配置文件

程序运行后，在同级目录自动生成两个配置文件：

- `task.json`：录制的操作数据
- `config.json`：应用配置（时间、速度、自启动等）

这些文件可备份或复制到其他机器上使用。

## 技术架构

### 系统要求

- **操作系统**：Windows 7 SP1 / 10 / 11 (x86/x64)
- **依赖**：无（纯绿色软件）
- **权限**：无需管理员权限

### 技术栈

- **语言**：Go 1.21+
- **GUI 框架**：lxn/walk (Win32 原生控件)
- **录制引擎**：Windows Hook API (`SetWindowsHookEx`)
- **回放引擎**：Windows Input API (`SendInput`)
- **数据存储**：本地 JSON 文件

### 架构设计

```
┌─────────────────────────────────────────┐
│           Presentation Layer            │
│         (Walk GUI Framework)            │
├─────────────────────────────────────────┤
│           Core Logic Layer              │
│  ┌──────────┬──────────┬──────────┐    │
│  │ Recorder │ Player   │ Scheduler│    │
│  │  (Hook)  │(SendInput)│ (Timer) │    │
│  └──────────┴──────────┴──────────┘    │
├─────────────────────────────────────────┤
│        Persistence Layer                │
│         (JSON Storage)                  │
└─────────────────────────────────────────┘
```

## 开发指南

### 构建环境

**macOS/Linux:**

```bash
# 安装依赖
brew install go mingw-w64  # macOS
# 或
sudo apt-get install golang gcc-mingw-w64  # Linux

# 克隆仓库
git clone https://github.com/yourusername/dailyflow.git
cd dailyflow

# 构建
./build/build.sh
```

**GitHub Actions:**

项目配置了 GitHub Actions 自动构建，每次推送代码到 main 分支或创建 tag 时自动编译 Windows 可执行文件。

构建产物可在 Actions 页面下载，或在 Releases 中获取。

### 项目结构

```
DailyFlow/
├── cmd/
│   └── dailyflow/          # 主程序入口
│       ├── main.go
│       └── dailyflow.manifest
├── internal/
│   ├── core/               # 核心逻辑
│   │   ├── hook.go         # 录制引擎
│   │   ├── simulator.go    # 回放引擎
│   │   └── scheduler.go    # 调度器
│   ├── model/              # 数据模型
│   │   ├── task.go
│   │   └── config.go
│   ├── storage/            # 持久化
│   │   └── persistence.go
│   └── ui/                 # 界面
│       ├── mainwindow.go
│       └── tray.go
├── build/
│   └── build.sh            # 构建脚本
├── .github/
│   └── workflows/
│       └── build.yml       # GitHub Actions 配置
├── go.mod
├── README.md
└── TESTING.md              # 测试指南
```

### 开发注意事项

1. **DPI Awareness**：已在 manifest 中配置，防止高分屏坐标偏移
2. **内存安全**：所有 Hook 使用 `defer` 确保释放
3. **免杀优化**：编译时使用 `-ldflags "-s -w"` 去除调试信息
4. **不使用网络**：代码中不引用任何网络库

## 常见问题

### Q: 为什么录制后回放坐标不准？

**A:** 可能是以下原因：

1. 录制和回放时屏幕分辨率不同
2. 系统 DPI 缩放设置不同
3. 高分屏未正确设置 DPI Awareness

**解决方案：** 确保录制和回放在相同的环境下进行。

### Q: 为什么有时回放失败？

**A:** 可能原因：

1. 系统响应慢，操作未完成就执行下一步
2. 窗口未完全加载

**解决方案：** 将速度调慢到 0.5x - 0.8x。

### Q: 能否在多台电脑上使用同一个录制文件？

**A:** 可以，但需要确保：

1. 屏幕分辨率相同
2. 操作的软件安装路径和界面布局相同

### Q: 程序是否会泄露密码？

**A:** 

- 程序会记录按键码（包括密码输入），但以明文存储在本地 JSON 文件中
- **不建议**在包含敏感信息的流程中使用
- 或在录制时跳过密码输入步骤，手动输入

### Q: 为什么需要保持屏幕常亮？

**A:** 

- 锁屏或休眠会中断回放
- Windows 系统在锁屏状态下无法正常执行输入模拟

## 许可证

本项目采用 [MIT License](LICENSE) 开源协议。

## 免责声明

⚠️ **重要提示**：

1. 本软件会录制并存储用户的鼠标和键盘操作（包括按键码），但不进行加密
2. 请勿在涉及敏感信息（如密码、银行账号）的场景中使用
3. 使用本软件产生的任何后果由使用者自行承担
4. 本软件仅供学习和合法用途使用

## 贡献

欢迎提交 Issue 和 Pull Request！

## 联系方式

如有问题或建议，请通过以下方式联系：

- GitHub Issues: [提交问题](https://github.com/yourusername/dailyflow/issues)
- Email: your.email@example.com

---

<div align="center">

Made with ❤️ for automation enthusiasts

</div>

